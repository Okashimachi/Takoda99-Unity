# DLL はバイナリとして扱う（テキストとして差分・マージを試みさせない）。
# Unity へ配置する Takoda99.Client.dll はビルド成果物をコミットしている
# （Unity/docs/.sdd/07-purecs-dll-reference.md §5）。コンフリクトしたら
# 手で解決せず、dotnet test を実行し直して生成し直すこと。
*.dll binary
*.pdb binary
